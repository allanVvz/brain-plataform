# Local dashboard against the Docker Compose backend.
# Browser calls /api-brain/* and Next rewrites it server-side to API_INTERNAL_BASE_URL.
# Docker Compose publishes the api service on :8080 (docker-compose.yml -> API_PORT).
API_INTERNAL_BASE_URL=http://localhost:8080
NEXT_PUBLIC_API_BASE_URL=/api-brain
