# Canonical conversation template

`persona-conversation-template.json` is the sole provisionable conversation
workflow.  The file is physically owned by this app so control-plane can
provision it by immutable checksum without keeping a fork.
