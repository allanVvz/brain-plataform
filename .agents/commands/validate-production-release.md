Use the `validate-production-release` skill. Run only the read-only production
audit, compare the intended SHA and digests, and report each hard gate. Do not
deploy, restart, resume agents, send messages, restore data or delete files.
Do not require WA Validator or conversational soak for the audit verdict.
