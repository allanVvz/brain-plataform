name: Legacy gateway image build disabled
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Gateway images are built from apps/gateway by build-monorepo-images.yml."
