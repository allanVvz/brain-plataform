# API runs through Docker Compose.
# Use the repository-level `.env.compose` generated by:
#
#   python infra/generate_keys.py --write .env.compose
#
ENVIRONMENT=qa
ALLOWED_ORIGINS=http://localhost:3000
RUN_EMBEDDED_WORKERS=false
