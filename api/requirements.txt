fastapi>=0.111.0
uvicorn>=0.29.0
gunicorn>=22.0.0
supabase>=2.4.0
httpx>=0.27.0
pydantic>=2.7.0
python-dotenv>=1.0.0
anthropic>=0.26.0
openai>=1.0.0
apscheduler>=3.10.0
gspread>=6.1.0
google-auth>=2.29.0
cryptography>=43.0.0
certifi>=2024.7.4
truststore>=0.10.0
pyyaml>=6.0.0
python-multipart>=0.0.9
# Asset pipeline — local classifier + PDF text extraction.
pillow>=10.0.0
pillow-heif>=0.16.0
pypdf>=4.0.0
# Optional local OCR backends (install in prod via Dockerfile if desired):
#   paddleocr, easyocr, pytesseract — selected at runtime via ASSET_OCR_BACKEND env.
# CI runs with backend=mock so no OCR lib is required for tests.
